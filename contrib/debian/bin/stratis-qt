#!/bin/sh

set -e

umask 077

basedir=~/.hpxt
dbfile="$basedir/DB_CONFIG"
cfgfile="$basedir/hpxt.conf"

[ -e "$basedir" ] || mkdir "$basedir"

# HPXT does not clean up DB log files by default
[ -e "$dbfile" ] || echo 'set_flags DB_LOG_AUTOREMOVE' > "$dbfile"

exec /usr/lib/hpxt/hpxt-qt "$@"
